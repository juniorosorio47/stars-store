import styled from 'styled-components';

export const Button = styled.button`
    border: none; 
    padding: 12px 16px;
    font-size: 16px; 
    cursor: pointer;

    >svg{ 
        pointer-events: none;
    }
`;
